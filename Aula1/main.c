#include <stdio.h>   // Biblioteca responsável por operações de entrada e saída, como o printf
#include <stdlib.h>  // Biblioteca que contém funções utilitárias, como o system

int main(int argc, char const *argv[])
{
    // Imprime a mensagem "Hello Word"
    printf("Hello Word \n");

    // Pausa a execução do programa até que o usuário pressione uma tecla
    system("pause");

    return 0;
}
